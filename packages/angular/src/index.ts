export { ThemeService, type ThemeContext } from './theme.service';

// Re-export core types and classes
export type {
  DesignTokens,
  Theme,
  ThemeConfig,
  TokenValue,
  TokenExportOptions,
  TokenParserOptions,
} from '@tokiforge/core';

export {
  ThemeRuntime,
  TokenParser,
  TokenExporter,
  ColorUtils,
} from '@tokiforge/core';


name: Deploy Documentation

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'documentation/**'
      - '.github/workflows/docs.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install root dependencies
        run: npm install

      - name: Build core package
        run: npm run build:core

      - name: Install documentation dependencies
        run: |
          cd documentation
          echo "Current directory: $(pwd)"
          echo "Checking for package-lock.json..."
          if [ -f package-lock.json ]; then
            echo "package-lock.json exists"
            if [ -s package-lock.json ]; then
              echo "package-lock.json is not empty"
              if grep -q '"lockfileVersion"' package-lock.json; then
                echo "package-lock.json has valid lockfileVersion"
                echo "Running npm ci..."
                npm ci || {
                  echo "npm ci failed, falling back to npm install"
                  npm install
                }
              else
                echo "package-lock.json missing lockfileVersion, using npm install"
                npm install
              fi
            else
              echo "package-lock.json is empty, using npm install"
              npm install
            fi
          else
            echo "package-lock.json not found, using npm install"
            npm install
          fi

      - name: Build documentation
        env:
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          cd documentation
          npm run build

      - name: Setup Pages
        uses: actions/configure-pages@v4
        id: pages
        continue-on-error: true
        with:
          enablement: false

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: documentation/.vitepress/dist
        continue-on-error: true
        if: always()

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        continue-on-error: true
        if: always()

